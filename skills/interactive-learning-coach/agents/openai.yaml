interface:
  display_name: "Interactive Learning Coach"
  short_description: "Roadmapped tutoring with progress-aware review"
  default_prompt: "Use $interactive-learning-coach to inspect the material, create a grounded roadmap, teach one minimal closed loop, track progress, support return sessions, save notes, schedule review, and create practice projects."
